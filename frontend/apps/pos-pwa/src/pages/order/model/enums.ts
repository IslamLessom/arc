export enum OrderTab {
  Check = 'check',
  Client = 'client',
}

export enum OrderStatus {
  Draft = 'draft',
  Confirmed = 'confirmed',
  Preparing = 'preparing',
  Ready = 'ready',
  Paid = 'paid',
  Cancelled = 'cancelled',
}
