# Статус реализации регистрации и авторизации

## ✅ Что реализовано

### 1. Модели данных
- ✅ `SubscriptionPlan` - тарифные планы (Free Trial 14 дней и другие)
- ✅ `Subscription` - подписки пользователей с датами начала/окончания
- ✅ `User` - обновлен: добавлены `EstablishmentID`, `OnboardingCompleted`, `SubscriptionID`
- ✅ `OnboardingQuestion` - вопросы опросника
- ✅ `OnboardingResponse` - ответы пользователя
- ✅ `OnboardingAnswer` - ответ на конкретный вопрос

### 2. Утилиты
- ✅ `pkg/auth/jwt.go` - генерация и валидация JWT токенов
- ✅ `pkg/auth/password.go` - хеширование и проверка паролей (bcrypt)

### 3. Репозитории
- ✅ `UserRepository` - добавлен метод `Update`
- ✅ `RoleRepository` - получение ролей
- ✅ `SubscriptionRepository` - работа с подписками
- ✅ `OnboardingRepository` - работа с вопросами и ответами

### 4. Use Cases
- ✅ `Register` - регистрация с автоматическим созданием подписки
- ✅ `Login` - авторизация с JWT токенами
- ✅ `RefreshToken` - обновление токенов
- ✅ `ValidateSubscription` - проверка активной подписки
- ✅ `GetQuestions` - получение вопросов опросника
- ✅ `SubmitAnswers` - обработка ответов и создание заведения

### 5. Handlers
- ✅ `POST /api/v1/auth/register` - регистрация
- ✅ `POST /api/v1/auth/login` - авторизация
- ✅ `POST /api/v1/auth/refresh` - обновление токена
- ✅ `POST /api/v1/auth/logout` - выход
- ✅ `GET /api/v1/auth/onboarding/questions` - получение вопросов
- ✅ `POST /api/v1/auth/onboarding/submit` - отправка ответов
- ✅ `GET /api/v1/auth/onboarding/response` - получение ответов пользователя

### 6. Middleware
- ✅ `Auth` - проверка JWT токена (обновлен для использования утилиты)
- ✅ `SubscriptionCheck` - проверка активной подписки

## 🔄 Полный flow регистрации

### Шаг 1: Регистрация
```
POST /api/v1/auth/register
{
  "email": "user@example.com",
  "password": "password123",
  "name": "Иван Иванов"
}
```

**Что происходит:**
1. Проверяется, что пользователь с таким email не существует
2. Хешируется пароль (bcrypt)
3. Получается роль "owner" из БД
4. Создается пользователь с `OnboardingCompleted = false`
5. Создается подписка "Free Trial" на 14 дней
6. Генерируются access_token и refresh_token
7. Возвращаются токены и данные пользователя

### Шаг 2: Получение вопросов опросника
```
GET /api/v1/auth/onboarding/questions
```

**Ответ:** Вопросы, сгруппированные по шагам

### Шаг 3: Прохождение опросника
```
POST /api/v1/auth/onboarding/submit
Authorization: Bearer <access_token>
{
  "answers": {
    "establishment_name": "Кафе Москва",
    "address": "ул. Тверская, д. 1",
    "phone": "+7 999 123-45-67",
    "type": "cafe",
    "has_seating_places": true,
    "table_count": 10,
    "has_takeaway": true,
    "has_delivery": false
  }
}
```

**Что происходит:**
1. Валидируются обязательные поля
2. Создается `Establishment` с настройками
3. Если есть сидячие места, создаются столы
4. Сохраняются ответы в `OnboardingResponse`
5. Устанавливается `User.OnboardingCompleted = true`
6. Связывается `User.EstablishmentID` с заведением

### Шаг 4: Использование приложения
Все защищенные endpoints требуют:
- JWT токен в заголовке `Authorization: Bearer <token>`
- Активную подписку (можно включить через middleware)

## ⚠️ Что нужно сделать перед запуском

### 1. Seed данные
Нужно создать seed скрипты для:
- **Роли**: `owner`, `manager`, `waiter`, `cook`
- **Тарифные планы**: `Free Trial` (14 дней, бесплатно)

Файлы готовы:
- `backend/internal/scripts/seed_roles_and_subscriptions.go`
- `backend/internal/scripts/seed_onboarding_questions.go`

### 2. Использование middleware проверки подписки
В `router.go` можно добавить проверку подписки для всех защищенных routes:
```go
protected.Use(middleware.SubscriptionCheck(usecases.Auth, logger))
```

Или применять выборочно для определенных групп endpoints.

### 3. Обработка ошибок роли
В `Register` есть проверка роли "owner". Если роли нет в БД, регистрация не пройдет. Нужно:
- Либо создать seed данные для ролей
- Либо сделать роль опциональной при регистрации

## 📋 Итоговый статус

✅ **Регистрация** - полностью реализована  
✅ **Авторизация** - полностью реализована  
✅ **Опросник** - полностью реализован  
✅ **Подписки** - модели и логика готовы  
⚠️ **Seed данные** - структура готова, нужно запустить скрипты  

**Система готова к использованию после создания seed данных!**
